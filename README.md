* `config.py` — глобальные настройки + загрузка из `config.ini`
* `io_module.py` — чтение `index.csv` и `parquet`
* `processing.py` — вычисления над `y/p`
* `output.py` — построение графиков, сохранение Png и показ окон
* `main.py` — точка входа
* `config.ini` — внешний конфиг (пути к данным, значене X)
